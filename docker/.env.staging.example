# Staging environment — copy to .env.staging and fill in values
# Usage: set -a && source docker/.env.staging && set +a && scripts/deploy.sh

# Required
DOMAIN=api.autoparts.example
POSTGRES_PASSWORD=CHANGE_ME_strong_random_password
API_KEYS=CHANGE_ME_generate_with_openssl_rand_hex_32

# Optional
# LOG_LEVEL=info
